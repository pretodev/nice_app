import 'package:flutter/material.dart';

class SearchExercisesPage extends StatelessWidget {
  const SearchExercisesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
        centerTitle: true,
      ),
      body: Container(),
    );
  }
}
